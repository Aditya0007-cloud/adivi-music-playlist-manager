{
  "name": "adivi-music-playlist-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Beatify - Spotify-inspired DSA Music Playlist Manager",
  "scripts": {
    "start": "concurrently \"npm run start --workspace backend\" \"npm run start --workspace frontend\"",
    "dev": "npm run start",
    "server": "npm run start --workspace backend",
    "client": "npm run start --workspace frontend",
    "build": "npm run build --workspace frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
